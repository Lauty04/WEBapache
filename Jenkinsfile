pipeline {
    agent any

    stages {
        stage('Ejecutar Comando pwd') {
            steps {
                echo 'Hola, este es un ejemplo de pipeline con "pwd"'
                sh 'pwd'
            }
        }
    }
}
