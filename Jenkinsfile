pipeline {
    agent any

    stages {
        stage('mover archivo') {
            steps {
                sh 'cp -r /var/jenkins_home/workspace/tarea2 /var/www/git/'
            }
        }
    }
}
